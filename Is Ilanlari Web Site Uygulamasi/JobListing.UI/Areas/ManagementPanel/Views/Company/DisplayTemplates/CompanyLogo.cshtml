@model byte[]

@if (Model != null)
{
    string imageBase64 = Convert.ToBase64String(Model);
    string imageSrc = string.Format("data:image/gif;base64,{0}", imageBase64);

    <img src="@imageSrc" class="rounded-circle" width="60" height="60" />
}